import * as React from 'react';

const MyActivity = (props:{}) => (
    <div>
        Neste componente seriam mostradas a lista de propostas e a lista de licitações do utilizador autenticado.
    </div>
);

export default MyActivity
